[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "helloworld"
version = "1.0.0"
description = "Python interface for the fputs Cython library function"
